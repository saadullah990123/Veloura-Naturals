# PostgreSQL connection string (Neon, Supabase, Railway, etc.)
DATABASE_URL=""

DB_POOL_MAX="5"

# Session signing secret — generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
SESSION_SECRET="4d27df1ac098f5ea55f7a38"

# Public site URL (used in metadata / OG tags)
NEXT_PUBLIC_SITE_URL="https://velouranaturals.co.uk"

# WhatsApp number customers reach you on, digits only with country code (no +, no spaces)
NEXT_PUBLIC_WHATSAPP_NUMBER="92300256825"
